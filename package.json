{
  "name": "chaos-td",
  "version": "0.0.1",
  "private": true,
  "description": "Chaos TD - Dual-lane competitive tower defense",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=apps/client",
    "dev:assets": "npm run dev --workspace=apps/asset-admin",
    "start:assets": "npm run start --workspace=apps/asset-admin",
    "build": "npm run build --workspace=@chaos-td/game-data && npm run build --workspace=@chaos-td/game-core && npm run build --workspace=@chaos-td/ai-strategy && npm run build --workspace=@chaos-td/ai-training && npm run build --workspace=@chaos-td/balance-sim && npm run build --workspace=@chaos-td/shared && npm run build --workspace=@chaos-td/test-fixtures && npm run build --workspace=@chaos-td/client",
    "lint": "eslint .",
    "typecheck": "tsc --project tsconfig.production.json --pretty false",
    "typecheck:test": "tsc --project tsconfig.test.json --pretty false",
    "test": "vitest run && npm run test --workspace=apps/asset-admin",
    "test:watch": "vitest",
    "docs:check-balance": "node scripts/check-balance.mjs",
    "docs:generate-counter-matrix": "npm run build --workspace=@chaos-td/game-data && node scripts/generate-counter-matrix.mjs",
    "balance:check": "npm run build --workspace=@chaos-td/game-data && npm run build --workspace=@chaos-td/game-core && node scripts/run-balance-smoke.mjs",
    "balance:simulate": "node scripts/run-balance-full.mjs",
    "balance:report": "npm run balance:simulate",
    "ai:check": "npm run build --workspace=@chaos-td/game-data && npm run build --workspace=@chaos-td/game-core && npm run build --workspace=@chaos-td/ai-strategy && npm run build --workspace=@chaos-td/ai-training && npx vitest run packages/ai-training/src/league.test.ts packages/ai-training/src/compat.test.ts",
    "ai:train": "npx vitest run packages/ai-training/src/league.test.ts packages/ai-training/src/fitness.test.ts packages/ai-training/src/hall-of-fame.test.ts",
    "ai:compatibility-check": "npm run build --workspace=@chaos-td/game-data && npm run build --workspace=@chaos-td/ai-strategy && npx vitest run packages/ai-training/src/compat.test.ts"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@types/node": "^22.13.0",
    "@typescript-eslint/eslint-plugin": "^8.64.0",
    "@typescript-eslint/parser": "^8.64.0",
    "eslint": "^10.7.0",
    "globals": "^17.7.0",
    "prettier": "^3.4.0",
    "typescript": "^6.0.0",
    "typescript-eslint": "^8.64.0",
    "vitest": "^4.1.0"
  },
  "engines": {
    "node": ">=24.0.0"
  },
  "packageManager": "npm@11.0.0",
  "lockfile": 3
}
